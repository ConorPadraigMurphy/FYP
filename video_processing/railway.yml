services:
  web:
    build:
      context: .
    ports:
      - 5000
    command: "gunicorn -w 4 -b 0.0.0.0:5000 app:app"

  worker:
    build:
      context: .
    command: "python3 video_consumer.py"
